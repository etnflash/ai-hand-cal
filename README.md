# Hand Cal

AI를 손계산으로 배우는 웹 앱. 행렬곱 → 선형층 → Softmax → Attention → Self-Attention.

## 실행

```bash
npm install
npm run dev
```

- 테스트: `npm test`
- 빌드: `npm run build`

브라우저에서 [http://localhost:3000](http://localhost:3000)
